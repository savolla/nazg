return {
  "nvim-neo-tree/neo-tree.nvim",
  opts = {
    window = {
      position = "right", -- "left", "right", "top", "bottom"
      width = 30, -- default is 40, try 25–30 for a narrower panel
    },
  },
}
